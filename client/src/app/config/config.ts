const server = "http://localhost:8080/meals-on-wheels";

const mealEndpointPrefix = "/meal";
const orderEndpointPrefix = "/order";
const customerEndpointPrefix = "/customer";

export const mealEndpoint = server + mealEndpointPrefix;
export const orderEndpoint = server + orderEndpointPrefix;
export const customerEndpoint = server + customerEndpointPrefix;